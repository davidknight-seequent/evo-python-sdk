# Core Jupyter widget runtime — works in JupyterLab, Notebook 7, VS Code and Colab.
anywidget>=0.9.13
traitlets>=5.9

# Evo Python SDK. This provides `evo.notebooks.ServiceManagerWidget`, the authenticated
# `connector`, and the `environment` (hub_url / org_id / workspace_id) used throughout.
# Package name may differ for your tenant; this is the public Seequent SDK distribution.
evo-sdk-common>=0.4

# Optional but recommended for local development / hot-reload of the front-end module.
# watchfiles>=0.21
